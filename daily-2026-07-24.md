# 每日進度記錄 — 2026-07-24

## 今日完成

- [x] **萃取 Thariq Shihipar「Finding Your Unknowns」方法論**：讀完 `D:\2026 Thariq_finding_unknown\` 下的原始素材（X 貼文、ABMedia 報導、兩份 YouTube 逐字稿）與已整理的分析筆記，重建出「地圖與領土」+「未知量四象限」（known knowns / known unknowns / unknown knowns / unknown unknowns）框架，以及對應的 5 個實務技巧（blindspot pass、brainstorm & prototype、interview、references、implementation notes、explainer & quiz）。
- [x] **先盤點既有 skill 庫再動手**：確認 `~/.claude/skills/` 裡的 `prototype`、`grilling` 已涵蓋部分技巧，避免重工。
- [x] **建立 7 個新 skill**（本地 `~/.claude/skills/` + push 到 `ckt520728/claude-skills`）：
  - `map-vs-territory` — 診斷入口，四象限框架 + routing table
  - `blindspot-pass` — 實作前盲點掃描
  - `reference-anchor` — 用具體參考物取代自然語言描述
  - `decision-first-plan` — 依決策易變性排序的實作計畫
  - `deviation-log` — 執行中容錯推進 + 偏差留痕
  - `comprehension-quiz` — 驗收前的決策報告 + 測驗閘門
  - `verify-with-rubric` — 用獨立 context 的 sub-agent + rubric 對抗自我參照偏誤
- [x] **交叉比對第二個獨立來源**：clone `ckt520728/finding-unknowns-skills`（fork 自 bozhouDev），發現它是單一 `CLAUDE.md` 行為準則檔（不是 SKILL.md 資料夾），萃取出 3 個補強：新增 `decision-first-plan` skill、`map-vs-territory` 補上「signs it's actually working」checklist 與「規格拉扯即未知量訊號」的中途觸發啟發式、其餘措辭銳化。
- [x] Repo commits：`289462f`（初版 6 skill）、`726071e`（補強）於 `ckt520728/claude-skills`。
- [x] 更新記憶：`user-repos-and-paths.md` 記錄新增 skill；新建 `github-bozhoudev-finding-unknowns-skills.md`（reference 型記憶）；`MEMORY.md` index 補一行。
- [x] 撰寫並存檔今日踩坑總結：`知識庫/2026 Claude code/2026-07-24_Finding-Unknowns-Skills_開發總結與踩坑紀錄.md`。
- [x] **（第二次 session）把 7 個 Finding Unknowns skill 打包進 GitHub repo 子資料夾**：在 `ckt520728/claude-skills` 建立 `Thariq_finding_unknown/` 資料夾，`git mv` 7 個 skill 進去，新增資料夾層級 README（含 stage/technique 對照表 + 「這是版控備份，不是 Claude Code 載入路徑，實際觸發要裝在 `~/.claude/skills/<name>/` 平鋪結構」的提醒），並更新根目錄 README 的路徑引用。Commit `67e8bae`，push 到 `origin/master`。本地 `~/.claude/skills/` 保持原本平鋪結構不動，避免破壞 skill 觸發。

## 卡住 / 未完成

- [ ] **Firebase 寫入**（原因：此 Claude Code session 沒有連 Firebase MCP，跟 `daily-2026-07-14.md`、`daily-2026-07-24.md`（第一次 session）記錄的坑一樣——這是**連續第三次**遇到，尚未解決根本問題）。
- [ ] 新的 7 個 skill 只在本次 session 手動安裝／push，尚未經過「重啟 Claude Code 後真的能被觸發」的驗證。

## 明日待辦

- [ ] 把 Firebase MCP 接上 Claude Code（已連續三次卡在這裡，值得優先處理，而不是每次都手動繞過）。
- [ ] 找一個真實情境（例如一個模糊的任務）實際觸發 `map-vs-territory` → `blindspot-pass` → `decision-first-plan` 整條鏈，驗證 skill 之間的 routing 是否真的順暢。
- [x] ~~視情況決定要不要把 7 個新 skill 打包成 `.skill` portable artifact~~ → 改成打包進 `Thariq_finding_unknown/` 子資料夾（見今日完成），本次選擇的是「資料夾分組」而非 `.skill` 單檔封裝。

## 技術筆記

（完整踩坑細節見同日 Second Brain 筆記：`知識庫/2026 Claude code/2026-07-24_Finding-Unknowns-Skills_開發總結與踩坑紀錄.md`）

- Kortex 匯出的來源檔可能只有 metadata、內文是空的——處理「已匯出材料」前先用 line count 粗篩，不要預設檔案存在＝內容存在。
- 不要用 repo 名稱猜打包格式：`finding-unknowns-skills` 這個名字看起來會跟自己的 `claude-skills` repo 同一種 `<name>/SKILL.md` 慣例，實際 clone 下去是完全不同的「單一 CLAUDE.md 行為準則」封裝。
- 跨系統產物（本地 skill 目錄 + GitHub repo）沒有自動同步，每次修改都要記得兩邊都覆蓋一次。
- **同日第二個 session 對同一份 daily note 要合併、不要覆蓋**：這份 `daily-2026-07-24.md` 是第一次 session 就建過的，這次直接讀完整份後在既有段落下追加，而不是重寫整個檔案，避免蓋掉第一次 session 的紀錄（Obsidian 端也沒有版本控制，蓋掉就真的沒了）。
